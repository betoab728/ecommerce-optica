import { config } from "@fortawesome/fontawesome-svg-core";
import "@fortawesome/fontawesome-svg-core/styles.css"; // Importa los estilos
config.autoAddCss = false; // Evita la auto-inserción de CSS por FontAwesome

export default function FontAwesomeConfig() {
  return null;
}